# Copy to .env for local `npm start`. Do not commit real keys.
# On the host, set these in the environment instead:
#   Vercel → Project Settings → Environment Variables
#   (or the equivalent Environment tab on Render / other Node hosts)
DVLA_API_KEY=
MOT_API_KEY=
